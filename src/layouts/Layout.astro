---
import "@styles/global.css";
import { Header, Footer } from "@components";

export interface Props {
  title: string;
  showThemeSwitcher?: boolean;
}

const { title, showThemeSwitcher = false } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Ángela Rera's Web Dev Blog" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <Header showThemeSwitcher={showThemeSwitcher} />

    <main>
      <slot />
    </main>

    <Footer />

    <!-- Theme manager initializing -->
    <script>
      window.addEventListener("DOMContentLoaded", () => {
        import("@scripts/theme-client.ts");
      });
    </script>
  </body>
</html>
